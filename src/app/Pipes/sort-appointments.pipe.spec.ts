import { SortAppointmentsPipe } from './sort-appointments.pipe';

describe('SortAppointmentsPipe', () => {
  it('create an instance', () => {
    const pipe = new SortAppointmentsPipe();
    expect(pipe).toBeTruthy();
  });
});
