import { MainpulateTimesPipe } from './mainpulate-times.pipe';

describe('MainpulateTimesPipe', () => {
  it('create an instance', () => {
    const pipe = new MainpulateTimesPipe();
    expect(pipe).toBeTruthy();
  });
});
